---
title: "工具應用筆記"
slug: "application"
description: "各種工具的應用場景與自訂模式筆記，記錄怎麼把工具接進實際工作流"
tags: ["雜項", "工具技巧", "工作流"]
---

這個資料夾收錄**各種工具的應用場景與自訂模式筆記** — 重點在「怎麼判斷哪些場景該交給這個工具、以及自己長出來的設定/模式」，工具安裝教學是次要的。

典型內容例如滑鼠手勢選單要接進哪些工作流、以及自訂選單的組織方式。

本分類的 permalink 模板是 `/other/:slug/`，會把子目錄層攤平——這裡的文章實際發布在 `/other/` 底下而非 `/other/application/`，因此手寫的相對連結兩邊都對不上（`mdtools` 按檔案路徑解析、Hugo 按模板）。文章清單交給下方的自動列表，它用的是 Hugo 實際產生的 URL。

---

底下自動列出本資料夾的所有文章、依日期排序。
