---
title: "1.1 宣告式的紅利與代價：三種順序各自由誰決定"
date: 2026-08-31
description: "同一個查詢兩種寫法跑出同樣結果、想知道引擎到底照哪一個順序做時"
weight: 1
tags: ["sql", "declarative", "query-plan", "optimizer"]
---

SQL 寫的是要什麼結果，不是怎麼算出來。這一條是這個語言其餘每個特徵的來源，包括它最容易讓人讀錯的那些。

紅利是描述與實作脫鉤。一段查詢在一萬列與一億列的資料上都不必改寫，換一台機器、換一個索引配置也不必改寫，因為怎麼算從來就不在寫的東西裡面。代價是同一件事的另一面——怎麼算不在寫的東西裡面，所以從查詢的文字看不出它要花多少。

## 三種順序

**書寫順序**是子句在文字上的先後：`SELECT` 在最前面，`FROM` 跟在後面，`WHERE`、`GROUP BY`、`HAVING`、`ORDER BY` 依序往下。這個順序是給人讀的，它的作用是讓查詢讀起來像一句話。

**求值順序**是語意模型規定的先後。它與書寫順序不同：先由 `FROM` 決定要處理哪個資料集合，再用 `WHERE` 篩掉列，接著 `GROUP BY` 分組、`HAVING` 篩掉組，最後才輪到 `SELECT` 把要輸出的欄位算出來、`ORDER BY` 排序。寫在最前面的 `SELECT` 在語意上排在倒數第二步。

**執行順序**是引擎實際做的事，由 [query optimizer](/sql/knowledge-cards/query-optimizer/) 決定，而它只受一個約束：輸出的列集合要與語意模型算出來的一致。在這個約束之內，它可以任意重排、可以換演算法、可以臨時建資料結構。

這三者不一致是宣告式這個選擇本身帶來的。讀查詢時的多數誤判來自把其中兩種當成同一種——最常見的是拿書寫順序去猜執行順序，以為「寫在前面的表會先被讀」。

## 執行順序與書寫順序無關

拿書店的兩張表來看這件事。顧客表五十列，訂單表二十萬列，要數的是兩者配得上的訂單有幾筆。同一個問題寫成兩種順序：

```sql
SELECT count(*) FROM 訂單 JOIN 顧客 ON 訂單.顧客編號 = 顧客.顧客編號;
SELECT count(*) FROM 顧客 JOIN 訂單 ON 訂單.顧客編號 = 顧客.顧客編號;
```

兩者都回 `200000`，而它們的 [query plan](/sql/knowledge-cards/query-plan/) 也一模一樣，SQLite 兩次都回報同一組步驟：

```text
SCAN 訂單
BLOOM FILTER ON 顧客 (顧客編號=?)
SEARCH 顧客 USING AUTOMATIC COVERING INDEX (顧客編號=?)
```

這段輸出說了兩件事。第一，順序與書寫位置無關——引擎兩次都選擇掃二十萬列那張、對五十列那張做查找，寫誰在前面沒有影響。第二，**計畫裡有查詢文字完全沒提到的東西**：`AUTOMATIC COVERING INDEX` 是 SQLite 當場替顧客表建的索引，`BLOOM FILTER` 是它加的過濾步驟。查詢裡沒有一個字要求這些，它們是引擎自己的決定。

「先讀的那張表」這個直覺因此對應不到任何真實的東西。文字上的先後只決定語意——哪一側的列受保護，見 [1.3](/sql/join-left-operand-accumulates/)——而不決定執行。

## 求值順序不是建議，它擋得住東西

語意模型摸不著，但它會在一個地方現形。書店要找出下過一張以上訂單的顧客，把「訂單張數大於一」這個條件寫進 `WHERE`：

```sql
SELECT 顧客編號 FROM 訂單 WHERE COUNT(*) > 1 GROUP BY 顧客編號;
```

SQLite 回 `misuse of aggregate: COUNT()`，DuckDB 回 `WHERE clause cannot contain aggregates`。兩個引擎都拒絕，而理由就寫在求值順序裡：`WHERE` 這一步分組還沒發生，沒有組就沒有組的計數可以拿來比。

同一個問題移到 `HAVING`，兩邊都回 `(1, 2)`——顧客編號 1 的佳穎，兩張訂單。分組這時候已經完成，每一組的計數算得出來了。

這組錯誤訊息是這個語言少數把內部模型直接講出來的地方。**它拒絕的理由是這一步拿不到那個值**，與這個寫法好不好看無關。

## 代價的位置

宣告式把「怎麼算」交給引擎，代價也跟著搬到引擎那一側。查詢的文字裡沒有任何東西表示它會掃全表還是走索引、會不會臨時建一個索引、會不會把中間結果寫到磁碟——上面那段計畫證明了這一點，那三個步驟一個都不在查詢裡。

所以驗證分成兩件互相獨立的事。**結果對不對**看輸出，從查詢本身推得出來。**代價可不可以接受**看計畫，非得問引擎不可，而且同一段查詢在不同資料量、不同索引、不同統計資訊底下的答案不一樣。

練習平台驗的是前一件。題目的資料量小到任何寫法都夠快，所以通過與否只反映描述得對不對；後一件在那個環境裡沒有東西可以量。

## 往下走

**求值順序的完整清單與它的例外**：[1.2 子句的求值順序](/sql/clause-evaluation-order/) 逐步走過從 `FROM` 到 `ORDER BY` 每一步手上有什麼，並分開兩種限制——語意模型規定的（各家引擎都擋）與各家自己的寬鬆度（同一段查詢換引擎會換行為，其中一種還不報錯）。

**書寫位置真正決定的那件事**：[1.3 JOIN 的左邊是累積結果](/sql/join-left-operand-accumulates/) 寫多張表相連時每個 `JOIN` 的左邊到底是什麼，以及在鏈中間放 `RIGHT JOIN` 會把保護範圍縮到只剩一張表。

**在真實系統上讀計畫**：[PostgreSQL Query Optimization](/backend/01-database/vendors/postgresql/query-optimization/) 給 `EXPLAIN` / `EXPLAIN ANALYZE` / `auto_explain` 三層工具的分工，以及統計資訊過時、多欄統計缺失這類讓計畫選錯的實際案例。
