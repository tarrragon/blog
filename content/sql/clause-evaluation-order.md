---
title: "1.2 子句的求值順序，以及哪些限制擋得掉哪些擋不掉"
date: 2026-08-31
description: "查詢報錯說某個名字用不了、想知道是這一步真的拿不到還是換個引擎就過時"
weight: 2
tags: ["sql", "clause-order", "having", "group-by", "alias"]
---

求值順序是語意模型規定的先後：`FROM` 決定要處理哪個資料集合、`WHERE` 篩掉列、`GROUP BY` 分組、`HAVING` 篩掉組、`SELECT` 算出要輸出的欄位、`ORDER BY` 排序。它與書寫順序不同，寫在最前面的 `SELECT` 在這裡排在倒數第二。

這個順序不規定引擎照什麼次序做事（見 [1.1](/sql/declarative-not-procedural/)）。它規定的是**每一步手上有什麼**——而手上沒有的東西，那一步就用不了。

## 每一步手上有什麼

`FROM` 走完之後手上是一個 [relation](/sql/knowledge-cards/relation/)，欄位是來源表的欄位。`WHERE` 因此只能引用這些欄位，一列一列判斷留不留。

`GROUP BY` 之後手上的單位從列換成組，這是整個順序裡變化最大的一步。原本每一列各自獨立，之後每一組是一個東西，而組裡那些列的個別值不再直接可用。

`HAVING` 篩掉的是組，所以它能用聚合函數——每一組的計數、總和、最大值到這一步都算得出來了。`SELECT` 到這裡才把要輸出的欄位算出來，別名也是在這一步才誕生。`ORDER BY` 排的是已經算好的輸出，所以它用得到別名。

這條線走完之後，兩類限制就分得開了：有些東西是這一步真的拿不到，有些是照模型拿不到而引擎替使用者補上了。前者換任何引擎都一樣，後者換引擎會換行為。

## 真的拿不到的：把聚合放進 WHERE

書店要找出下過一張以上訂單的顧客。「訂單張數大於一」這個條件寫進 `WHERE`：

```sql
SELECT 顧客編號 FROM 訂單 WHERE COUNT(*) > 1 GROUP BY 顧客編號;
```

SQLite 回 `misuse of aggregate: COUNT()`，DuckDB 回 `WHERE clause cannot contain aggregates`。同一個問題移到 `HAVING`：

```sql
SELECT 顧客編號, COUNT(*) AS 張數 FROM 訂單 GROUP BY 顧客編號 HAVING COUNT(*) > 1;
-- 兩個引擎都回 (1, 2)：顧客編號 1 的佳穎，兩張訂單
```

差別在分組發生了沒有。`WHERE` 那一步手上還是一列一列的訂單，「這一組有幾張」這個值在那個時刻還不存在，所以這是模型層的限制而不是功能缺口。這個限制與引擎的寬鬆度無關。

## 引擎自己決定的：WHERE 用 SELECT 的別名

按求值順序，`WHERE` 早於 `SELECT`，別名那時候還沒誕生。標準照這個推論禁止，PostgreSQL 也照這個推論拒絕。

而 SQLite 與 DuckDB 都接受。書店查佳穎這一列，把姓名欄改名之後在 `WHERE` 用新名字：

```sql
SELECT 姓名 AS 客戶 FROM 顧客 WHERE 客戶 = '佳穎';
-- SQLite 與 DuckDB 都回 ('佳穎',)；PostgreSQL 報 column "客戶" does not exist
```

這是引擎給的方便，而它疊在語意模型上面，沒有取代它。可以從一個細節看出疊的位置：來源表本來就有同名欄位的時候，`WHERE` 綁的是來源欄位而不是別名。實測用一張只有 `x` 與 `y` 兩欄的表，把 `y` 取名成 `x` 之後在 `WHERE` 寫 `x = 1`，SQLite 與 DuckDB 都拿來源欄位 `x` 去比對，回傳那一列的 `y`。**別名只在名字沒被佔用時才補位。**

## 另一處引擎差異，而這一種不報錯

`GROUP BY` 之後取一個不在分組鍵裡的欄位。書店按顧客分組，卻要求輸出下單日：

```sql
SELECT 顧客編號, 下單日 FROM 訂單 GROUP BY 顧客編號;
```

DuckDB 拒絕，訊息是 `column "下單日" must appear in the GROUP BY clause`。SQLite 接受，回 `(1, '2026-03-02')`。

佳穎那一組有兩張訂單，下單日分別是 3 月 2 日與 3 月 9 日。SQLite 挑了第一張的日期回傳，而它沒說自己挑了哪一張，也不保證下次挑同一張。

**這一種的危害高於前一種，因為它不報錯。** 輸出看起來完全合理——每組一列、欄位齊全、日期是真實存在的日期，而那個值是從組裡任意挑的。前一種寫法在哪裡都跑不動，寫的當下就會發現；這一種在開發用的引擎上跑得動，發現的時機晚得多。

## 分辨兩種限制的方法

碰到「這個名字在這裡用不了」的時候，問這一步手上有沒有那個東西。分組還沒發生就要組的計數，是真的拿不到；投影還沒算就要別名，是照模型拿不到而有些引擎補上了。

差別落在可攜性。真的拿不到的那些會在第一次執行就擋下來，引擎補上的那些會安靜跑過，等到換引擎才浮現——而其中最麻煩的一種連換引擎都不報錯，只是換一個值回來。

## 往下走

**這個順序從哪裡來**：[1.1 宣告式的紅利與代價](/sql/declarative-not-procedural/) 寫求值順序與執行順序為什麼可以不一致，並用一段計畫證明引擎沒有照書寫順序做，還做了查詢裡完全沒提到的事。

**`FROM` 那一步實際做了什麼**：多張表的時候 `FROM` 按書寫位置逐個把表併進來。[1.3 JOIN 的左邊是累積結果](/sql/join-left-operand-accumulates/) 寫這個累積過程，以及它決定了每個 `ON` 引用得到哪些欄位。

**條件放 `ON` 還是放 `WHERE`**：這兩個位置落在求值順序的不同步。[1.4 ON 描述關係、WHERE 篩選結果](/sql/on-describes-where-filters/) 寫同一個條件放兩處在內連接下同值而在外連接下不同值的原因。
